surface_amount = size(sp_set, 1);

figure;
subplot(1,2,1);
imshow(Src);

result = zeros(x, y, 3);

for i=1:surface_amount
    point_amount = size(sp_set{i}, 1);
    temp(:, :, 1) = 0;
    temp(:, :, 2) = 0;
    temp(:, :, 3) = 0;
    for j=1:point_amount
        result(sp_set{i}(j,1), sp_set{i}(j,2), :) = Src(sp_set{i}(j,1), sp_set{i}(j,2), :);
        result(sp_set{i}(j,1), sp_set{i}(j,2), 3) = i/5;
    end
end    

subplot(1,2,2);
imshow(result);